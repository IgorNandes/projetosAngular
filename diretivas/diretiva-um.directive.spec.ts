import { DiretivaUmDirective } from './diretiva-um.directive';

describe('DiretivaUmDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaUmDirective();
    expect(directive).toBeTruthy();
  });
});
