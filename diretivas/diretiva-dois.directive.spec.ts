import { DiretivadoisDirective } from './diretiva-dois.directive';

describe('DiretivadoisDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivadoisDirective();
    expect(directive).toBeTruthy();
  });
});
